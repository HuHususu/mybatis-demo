<mapper namespace="product">
    <select id="selectList" resultType="com.husu.pojo.Product">
        select * from product
    </select>

    <select id="selectOne" resultType="com.husu.pojo.Product">
        select * from product where id = ? and username = ?
    </select>

</mapper>